# CleanOps frontend

Expo Router app. The real setup steps live in the repo root [README](../README.md).

```bash
cp .env.example .env
yarn install
yarn start
```

`EXPO_PUBLIC_BACKEND_URL` must point at the FastAPI server (default `http://localhost:8000`).
